class Solution(object):
    def buildArray(self, nums):
        n=len(nums)
        ans=[]
        for i in range(n):
            ans.append(nums[nums[i]])
        return ans
sol=Solution()
nums=[0,2,1,5,3,4]
print(sol.buildArray(nums))
        
